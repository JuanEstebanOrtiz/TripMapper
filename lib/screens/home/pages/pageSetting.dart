import 'package:flutter/material.dart';

class PageSetting extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Page Setting', textScaleFactor: 2.0),
    );
  }
}